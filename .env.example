# LLM API Key
# Set this to use an OpenAI-compatible provider without storing the key in config.yaml.
# If unset, the app will fall back to the api_key field in config.yaml (not recommended).
LLM_API_KEY=
